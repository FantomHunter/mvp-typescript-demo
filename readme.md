# Demo MVP pattern in typescript

## command list:
```bash 
npm init -y
npm install parcel-bundler typescript --save-dev
npx tsc --init
```

add to package.json
``` json
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html"
  },
````